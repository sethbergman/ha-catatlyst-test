// import math-sqrt from 'math';
export class Math {
	import math-sqrt from 'math';
	constructor()
	let value = (x) => sqrt()
	let x = Date.now()
	let y = (sqrt(x))
	console.log(x)
	console.log(y)
	sqrt(y)
}
