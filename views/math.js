import * from 'math-sqrt';
export class Math {
Math.sqrt(2).then({Date.now()})
window.alert(expect(2).toBe(Math.[...Array(length).keys()]);)
}

var sqrt = require('math-sqrt');

Math.sqrt(2);
Date.now(2);

// Math.sqrt(1);  // 1
// Math.sqrt(0);  // 0
// Math.sqrt(-1); // NaN
\forall x \geq 0, \mathtt{Math.sqrt(x)} = \sqrt{x} = \text{the unique} \; y \geq 0 \; \text{such that} \; y^2 = x
